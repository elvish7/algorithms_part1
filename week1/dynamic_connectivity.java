/**
Dynamic connectivity
The algorithm of how to solve dynamic connectivity problems
Methods of union and connect (to actually run the code) is the next topic
 */

/*
public static void main(String[] args){
	int N = StdIn.readInt();
	UF uf = new UF(N);
	while (!StdIn.isEmpty()){
		int p = StdIn.readInt();
		int q = StdIn.readInt();
		if (!uf.connected(p, q)){
			uf.union(p, q);
			StdOut.println(p + " " + q);
		}
	}
}
*/